<div class="eosf-basic-citations">
    {{#each citations as |c|}}
        <h4> {{c.label}}</h4>
        {{# if c.error}}
            <p class="text-danger text-smaller">{{c.error}}</p>
        {{else}}
            <p>{{c.citation}}</p>
        {{/if}}
    {{/each}}
</div>
